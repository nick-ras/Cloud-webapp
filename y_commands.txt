

source .env
source env/bin/activate
export APP_SETTINGS=config.DevelopmentConfig
python3 app.py run

source .env
source env/bin/activate
export APP_SETTINGS=config.DockerConfig
python3 app.py docker

source .env
source env/bin/activate
export APP_SETTINGS=config.TestingConfig
python3 app.py test

source .env
pytest tests/test_mock_views.py

#systemlevel installs
sudo apt-get update && sudo apt-get install -y --no-install-recommends gnupg2 curl && curl https://packages.microsoft.com/keys/microsoft.asc | sudo apt-key add - && curl https://packages.microsoft.com/config/debian/10/prod.list | sudo tee /etc/apt/sources.list.d/mssql-release.list > /dev/null && sudo apt-get update && sudo ACCEPT_EULA=Y apt-get install -y --no-install-recommends msodbcsql17 unixodbc-dev && sudo rm -rf /var/lib/apt/lists/*

#Docker
docker build -t nickras99/webapp1:latest . && docker run -p 80:80 --env-file .env -e APP_SETTINGS=config.DevelopmentConfig nickras99/webapp1:latest
docker push nickras99/webapp1:5.0

#check whats inside
docker run -it nickras99/webapp1:5.0 /bin/bash

#-d is see inside container

#volumes - for quick changes thats not build but just in normal dir
-v $(/home/VMNick1/webapp2):/app 