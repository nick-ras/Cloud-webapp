

source .env
source env/bin/activate
export APP_SETTINGS=config.DevelopmentConfig
python3 app.py run

source .env
source env/bin/activate
export APP_SETTINGS=config.DockerConfig
python3 app.py docker

source .env
source env/bin/activate
export APP_SETTINGS=config.TestingConfig
python3 app.py test

source .env
pytest tests/test_mock_views.py

#Docker
docker build -t nickras99/webapp1:latest . && docker run -p 5000:5000 --env-file .env -e APP_SETTINGS=config.DevelopmentConfig nickras99/webapp1:latest

docker push nickras99/webapp1:5.0

#check whats inside
docker run -it nickras99/webapp1:5.0 /bin/bash

#-d is see inside container

#volumes - for quick changes thats not build but just in normal dir
-v $(/home/VMNick1/webapp2):/app 